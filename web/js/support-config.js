/* Support link shown as a small, muted line in the footer — not a banner,
   popup, or anything that interrupts the app. Toggle `enabled` or change
   the URL/label here; nothing else needs to change. */

var SUPPORT_LINK = {
  enabled: false,
  url: "https://www.buymeacoffee.com/YOUR_USERNAME",
  label: "Buy me a coffee"
};
